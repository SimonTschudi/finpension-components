import { Counter } from '@components/counter/Counter';

export const AboutPage = () => {
    return (
        <div>
            <h1>About</h1>
            <p>Welcome to the about page!</p>
            <Counter />
        </div>
    );
};
